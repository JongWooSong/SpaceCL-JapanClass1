Q.스레드는? 
A.멀티 처리이다.

Q.쓰레드를 만드는 방법2가지?
A.1)Thread 클래스 상속  ==> run() 메서드 오버라이딩
   2)Runnable 인터페이스 구현  ==> run() 메서드 구현(오버라이딩)

Q.스레드 실행?
A. new Thread().start()

Q.쓰레드 동기화는?
A.syncronized 예약어를 메서드 앞에 붙인다.